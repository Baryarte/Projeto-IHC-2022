import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
  },

  icon: {
    marginTop: 3,
    width:53,
    height:53,
    marginTop:20,
  },
});

export default styles;